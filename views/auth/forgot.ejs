<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %>- Ims Apps</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
</head>
<body class="min-h-screen bg-gradient-to-br from-blue-50 to-indigo-100">
    <div class="min-h-screen flex items-center justify-center py-12 px-4 sm:px-6 lg:px-8">
        <div class="max-w-md w-full space-y-8">
    <div class="bg-white rounded-lg shadow-lg p-8">
        <!-- Header -->
        <div class="text-center mb-8">
            <div class="mx-auto h-12 w-12 bg-indigo-600 rounded-full flex items-center justify-center">
                <i class="fas fa-cube text-white text-xl"></i>
            </div>
            <h2 class="mt-4 text-3xl font-bold text-gray-900">Lupa Password</h2>
            <p class="mt-2 text-sm text-gray-600">Silakan masukan no Whatsapp anda</p>
        </div>

        <!-- Error Alert -->
        <% if (error) { %>
        <div class="bg-red-50 border border-red-200 rounded-lg p-4 mb-6">
            <div class="flex">
                <i class="fas fa-exclamation-circle text-red-400 mr-2 mt-0.5"></i>
                <p class="text-sm text-red-700"><%= error %></p>
            </div>
        </div>
        <% } %>

        <!-- Form -->
        <form method="POST" action="/forgot" class="space-y-6">
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">

                <div>
                    <label for="nowa" class="block text-sm font-medium text-gray-700 mb-2">No Whatsapp</label>
                    <div class="flex space-x-2">
                        <input 
                            id="nowa" 
                            name="nowa" 
                            type="number" 
                            required 
                            class="flex-1 px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent"
                            placeholder="Masukkan no Whatsapp"
                        >
                        <button 
                            type="button" 
                            id="btn-minta-otp"
                            disabled
                            class="px-4 py-2 bg-indigo-400 text-white rounded-lg cursor-not-allowed focus:outline-none"
                        >
                            Minta OTP
                        </button>
                    </div>
                </div> <!-- Tutup div pembungkus No Whatsapp -->

                <div>
                    <label for="otp" class="block text-sm font-medium text-gray-700 mb-2">OTP</label>
                    <input 
                        id="otp" 
                        name="otp" 
                        type="number" 
                        required 
                        readonly
                        class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent"
                        placeholder="Masukkan no otp dari Whatsapp"
                    >
                </div>

                <div>
                    <label for="password" class="block text-sm font-medium text-gray-700 mb-2">Password Baru</label>
                    <div class="relative">
                        <input 
                            id="password" 
                            name="password" 
                            type="password" 
                            required 
                            class="w-full px-3 py-2 border border-gray-300 rounded-lg focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:border-transparent pr-10"
                            placeholder="Masukkan password"
                        >
                        <button type="button" onclick="togglePassword()" class="absolute inset-y-0 right-0 pr-3 flex items-center">
                            <i id="password-icon" class="fas fa-eye text-gray-400 hover:text-gray-600"></i>
                        </button>
                    </div>
                </div>

                <div class="flex items-center justify-end">
                    <a href="/login" class="text-sm text-indigo-600 hover:text-indigo-500">Sudah ingat password?</a>
                </div>

                <button 
                    type="submit" 
                    class="w-full flex justify-center py-3 px-4 border border-transparent rounded-lg shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500 transition duration-200"
                >
                    <i class="fas fa-key mr-2"></i>
                    Ubah Password
                </button>
            </form>

        </div>
    </div>
</body>
</html>

<script>
    const inputNowa = document.getElementById('nowa');
    const btnMintaOtp = document.getElementById('btn-minta-otp');
    inputNowa.addEventListener('change', function( event ) {
        console.log(event.target.value.length);
       
        if(event.target.value.length >= 10){
            // document.getElementById('otp').removeAttribute('disabled');
            btnMintaOtp.disabled = false;
        btnMintaOtp.classList.remove('bg-indigo-400', 'cursor-not-allowed');
        btnMintaOtp.classList.add('bg-indigo-600', 'hover:bg-indigo-700', 'cursor-pointer');

        } else {
             alert("nomor whatsapp tidak valid");
            // document.getElementById('otp').setAttribute('disabled', 'true');
        //     btnMintaOtp.disabled = false;
        // btnMintaOtp.classList.remove('bg-indigo-400', 'cursor-not-allowed');
        // btnMintaOtp.classList.add('bg-indigo-600', 'hover:bg-indigo-700', 'cursor-pointer');

        }
      
    });
    btnMintaOtp.addEventListener('click', function() {
        // Logic untuk mengirim OTP ke nomor WhatsApp
        document.getElementById('otp').removeAttribute('readonly');
        inputNowa.setAttribute('readonly', 'true');
        btnMintaOtp.disabled = true;
        btnMintaOtp.classList.remove('bg-indigo-600', 'hover:bg-indigo-700', 'cursor-pointer');
        btnMintaOtp.classList.add('bg-indigo-400', 'cursor-not-allowed');
        alert('OTP telah dikirim ke nomor WhatsApp Anda.');
        fetch('/api/forgot/send-otp', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'CSRF-Token': '<%= csrfToken %>'
            },
            body: JSON.stringify({ nowa: inputNowa.value })
        }).then(response => response.json())
        .then(data => {
            if(data.success) {
                // alert('OTP telah dikirim ke nomor WhatsApp Anda.');
            } else {
                // alert('Gagal mengirim OTP. Silakan coba lagi.');
            }
        }).catch(error => {
            console.error('Error:', error);
            // alert('Terjadi kesalahan. Silakan coba lagi.');
        });
    });

function togglePassword() {
    const passwordInput = document.getElementById('password');
    const passwordIcon = document.getElementById('password-icon');
    
    if (passwordInput.type === 'password') {
        passwordInput.type = 'text';
        passwordIcon.className = 'fas fa-eye-slash text-gray-400 hover:text-gray-600';
    } else {
        passwordInput.type = 'password';
        passwordIcon.className = 'fas fa-eye text-gray-400 hover:text-gray-600';
    }
}
</script>